import { questionPriority } from 'questions-answers/helpers/question-priority';
import { module, test } from 'qunit';

module('Unit | Helper | question priority');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionPriority([42]);
  assert.ok(result);
});
